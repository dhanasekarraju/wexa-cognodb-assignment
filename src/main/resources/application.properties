# Spring Boot Application Configuration
server.port=${PORT:8080}

# Application name
spring.application.name=TalentGraph

# Logging configuration
logging.level.org.springframework.boot=INFO
logging.level.com.wexa=INFO