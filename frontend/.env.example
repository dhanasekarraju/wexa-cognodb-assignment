# Vite Environment Variables
# These variables are exposed to the frontend code
# Only variables prefixed with VITE_ are available to the frontend

# Base URL for API calls
# During development, this is proxied to the backend
# In production, this should point to your deployed backend
VITE_API_BASE_URL=http://localhost:8080